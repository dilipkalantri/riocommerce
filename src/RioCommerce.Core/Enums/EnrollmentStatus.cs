namespace RioCommerce.Core.Enums;

// Derived (not persisted) — reflects whether an order's course access has been activated for the student.
public enum EnrollmentStatus { NotActivated, Active, Expired }
