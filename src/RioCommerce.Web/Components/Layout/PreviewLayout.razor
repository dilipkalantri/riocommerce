@inherits LayoutComponentBase
@* Chrome-less layout for the standalone design-preview pages (zip index.html / courses.html).
   No shared Header/Footer/ContactStrip — these pages carry their own, exactly like the zip,
   so they don't depend on or affect any existing public page. *@
@Body
